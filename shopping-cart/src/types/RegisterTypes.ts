export type registerTypes = {
    firstName : string,
    lastName : string,
    email : string,
    password : string,
    confirmPassword : string
}

export type loginType = {
    email : string,
    password : string,
    staySign : boolean
}